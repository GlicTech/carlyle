<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>EventDelayedFillInReason</fullName>
    <active>true</active>
    <description>This rule forces users to enter a Primary Delay and Delay Comment when the Actual Date is later than the Projected Date.</description>
    <errorConditionFormula>AND(
    pro_Actual_Date__c &gt; pro_Projected_Date__c,
    pro_Actual_Date__c &gt; DATE(2025, 1, 1),
    OR(
        ISBLANK(TEXT(pro_Primary_Delay__c)),
        ISBLANK(pro_Delay_Comment__c)
    )
)</errorConditionFormula>
    <errorMessage>The Actual Date is later than the Projected Date. The fields Primary Delay and Delay Comment must be populated.</errorMessage>
</ValidationRule>
