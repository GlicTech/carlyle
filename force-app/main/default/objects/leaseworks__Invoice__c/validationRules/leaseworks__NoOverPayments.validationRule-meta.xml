<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>leaseworks__NoOverPayments</fullName>
    <active>false</active>
    <description>Payment cannot Exceed total invoiced amount : This has been moved to trigger hence marking false and inactive</description>
    <errorConditionFormula>/*AND(!$Setup.LeaseWorksSettings__c.Disable_All_Triggers__c,
				!ispickVal(Invoice_Type__c,&apos;Interest&apos;),
				Total_Amount_Due__c &gt; 0,
				(Amount_Paid__c+Availed_Credit_Amount_Cm__c+
				if(ISBLANK(Deferred_Amount__c),0,Deferred_Amount__c))
		&gt;Total_Amount_Due__c)*/false</errorConditionFormula>
    <errorMessage>The amount paid/credited should be less than or equal to the total invoice amount</errorMessage>
</ValidationRule>
