<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>pro_Completion_Key_Date_Required</fullName>
    <active>true</active>
    <description>Prevents completing an item when Key Date is configured as a required completion field but has not been populated.</description>
    <errorConditionFormula>AND(
  ISPICKVAL(pro_Status__c, &quot;Completed&quot;),
  CONTAINS(pro_Completion_Fields__c, &quot;pro_Key_Date__c&quot;),
  ISBLANK(pro_Key_Date__c)
)</errorConditionFormula>
    <errorDisplayField>pro_Key_Date__c</errorDisplayField>
    <errorMessage>Date is required to complete this item.</errorMessage>
</ValidationRule>
