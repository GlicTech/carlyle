<?xml version="1.0" encoding="UTF-8"?>
<Flow xmlns="http://soap.sforce.com/2006/04/metadata">
    <apiVersion>61.0</apiVersion>
    <description>Updates the Geneva and Capetown Convention on the lease when there is a change on the checkboxes on the countries attached as habitual base or country of registry</description>
    <environments>Default</environments>
    <label>Update CP and Geneva cbk on lease when update on country</label>
    <processType>AutoLaunchedFlow</processType>
</Flow>
